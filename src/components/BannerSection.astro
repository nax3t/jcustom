---
import { Image } from 'astro:assets';
import promo1 from '../assets/images/promo-1.webp';
import promo2 from '../assets/images/promo-2.webp';
---

<section class="bg-dark text-white overflow-hidden relative grain">
  <div class="max-w-[1318px] mx-auto px-6 md:px-8 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-16">
      <!-- Images -->
      <div class="relative min-h-[400px] lg:min-h-[600px]">
        <div
          class="absolute top-12 left-0 w-[55%] z-10"
          x-data
          x-intersect.once="$el.classList.add('animate-scale-in')"
        >
          <Image
            src={promo1}
            alt="Luxurious tropical outdoor living space with modern pergola, built-in seating, water feature, and lush landscaping"
            class="shadow-2xl"
            widths={[300, 500, 700]}
            sizes="(max-width: 1024px) 55vw, 30vw"
          />
        </div>
        <div
          class="absolute bottom-12 right-0 w-[55%] z-20"
          x-data
          x-intersect.once="$el.classList.add('animate-scale-in')"
        >
          <Image
            src={promo2}
            alt="Modern outdoor lounge area with pergola, tropical plants, built-in seating, wooden table, and stepping stone pathway"
            class="shadow-2xl"
            widths={[300, 500, 700]}
            sizes="(max-width: 1024px) 55vw, 30vw"
          />
        </div>
      </div>

      <!-- Text -->
      <div class="flex items-center py-20 lg:py-36">
        <div x-data x-intersect.once="$el.classList.add('animate-fade-up')" class="opacity-0">
          <div class="section-label section-label-light mb-6">
            <span>Your Vision, Our Craft</span>
          </div>
          <h3 class="font-display text-[clamp(2rem,4vw,2.75rem)] leading-[1.15] tracking-[-0.01em] mb-6">
            Your personal oasis awaits.
          </h3>
          <p class="text-white/70 text-lg leading-relaxed mb-10">
            Refine your distinguished lifestyle with a bespoke outdoor sanctuary.
            Our expert artisans craft luxurious decks, patios, and al fresco kitchens,
            transforming your property into an exclusive haven for relaxation and entertainment.
          </p>
          <a href="#services" class="btn btn-outline">
            <span>View Services</span>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
